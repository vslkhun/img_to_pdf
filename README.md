## System Dependencies
This application requires **Poppler** to extract and process PDF pages.

- **Windows**: Run `winget install oschwartz10612.Poppler`
- **macOS**: Run `brew install poppler`
- **Linux**: Run `sudo apt-get install poppler-utils`
